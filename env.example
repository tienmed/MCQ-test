# Google Sheets Credentials
# Copy the WHOLE JSON content from your service account key file into this variable (surrounded by single quotes)
GOOGLE_SERVICE_ACCOUNT_KEY='{"type": "service_account", ...}'
GOOGLE_SHEET_ID='your_sheet_id_here'

# NextAuth Configuration
# You can generate a secret with `openssl rand -base64 32` or just use a random string
AUTH_SECRET='your_random_secret_here'

# Google OAuth Credentials (for Google Login)
# Get these from Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs
GOOGLE_CLIENT_ID='your_google_client_id.apps.googleusercontent.com'
GOOGLE_CLIENT_SECRET='your_google_client_secret'

# Base URL of your app (e.g., http://localhost:3000 for local, or your Vercel URL)
NEXTAUTH_URL='http://localhost:3000'
